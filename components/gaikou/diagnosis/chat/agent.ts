// チャット診断の案内役設定。
// 画像を差し替えるときは、public/images/ に「新しいファイル名」で追加して
// このパスだけ変更する（同名上書きはVercelの画像キャッシュが残るため避ける）。
export const CHAT_AGENT_IMAGE = "/images/chat-agent.png";

// 特定の人物名は使わない（写真確定後に変更可）
export const CHAT_AGENT_NAME = "外構プラン案内";
